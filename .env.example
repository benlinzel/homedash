# Path to the Docker socket inside the container
DOCKER_SOCKET_PATH=/var/run/docker.sock
# --- Docker & Sudo Permissions ---
# These IDs grant the container permissions on the HOST machine.

# The User ID of your user on the host server.
# Find with `id -u` on Linux/macOS.
UID=1000

# The Group ID of your user on the host server.
# Find with `id -g` on Linux/macOS.
GID=1000

# The Group ID of the Docker socket on the host server.
# Find with `getent group docker | cut -d: -f3` on Linux.
# Find with `stat -f '%g' /var/run/docker.sock` on macOS.
DOCKER_GID=999

# Your Cloudflare Tunnel token
# Replace this with your actual token from the Cloudflare dashboard
CLOUDFLARE_TUNNEL_TOKEN=YOUR_CLOUDFLARE_TUNNEL_TOKEN

# --- PWA Push Notifications ---
# Generate these once with `npx web-push generate-vapid-keys`
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_public_key_here
VAPID_PRIVATE_KEY=your_private_key_here
VAPID_EMAIL=your_email